{
  "name": "tangram-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter \"./packages/**\" build",
    "fmt": "prettier --write --cache \"**/*.[tj]s?(x)\""
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}