[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tangram_navaid"
version = "0.6.0"
description = "Navaid, fix and Field 15 route search plugin for tangram"
readme = "readme.md"
requires-python = ">=3.10"
license = "AGPL-3.0"
authors = [
    { name = "Xavier Olive", email = "git@xoolive.org" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Visualization",
]
dependencies = ["tangram_core>=0.6.0"]

[project.entry-points."tangram_core.plugins"]
tangram_navaid = "tangram_navaid:plugin"

[tool.uv.sources]
tangram_core = { workspace = true }

[tool.hatch.build.targets.sdist]
ignore-vcs = true
include = ["dist-frontend/*", "src/*", "package.json"]

[tool.hatch.build.targets.wheel.force-include]
"dist-frontend" = "tangram_navaid/dist-frontend"
"package.json" = "tangram_navaid/package.json"
